name: keepalive
on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Ping the bridge
        run: curl -sf https://telegram-bridge-25ww.onrender.com/health && echo "bridge awake"
